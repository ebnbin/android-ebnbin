<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ebnbin.ebapp">
    <application
        android:name=".EBAppApplication"
        android:icon="@mipmap/ebapp_icon"
        android:roundIcon="@mipmap/ebapp_icon"
        android:logo="@drawable/ebapp_logo"
        android:theme="@style/EBAppTheme">
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${firebaseCrashlyticsCollectionEnabled}"/>
    </application>
</manifest>
